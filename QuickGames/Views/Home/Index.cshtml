<div class="jumbotron">
	<h1>ASP.NET</h1>
	<p class="lead">ASP.NET is a free web framework for building great Web sites and Web applications using HTML, CSS, and JavaScript.</p>
	<p><a href="http://asp.net" class="btn btn-primary btn-lg">Learn more &raquo;</a></p>
</div>

<div ng-app="gameGrid" ng-controller="sequencedGridController as gridViewModel">
	Sequenced Grid: Currently looking for: {{gridViewModel.expectedCell.Value}}
	<div>
		<table style="border-collapse: separate; border-spacing: 5px;">
			<tr ng-repeat="row in gridViewModel.grid.rows">
				<td ng-repeat="cell in row"><a class="btn btn-primary" style="width:75px;height:75px;" ng-click="gridViewModel.onCellClicked(cell)">{{cell.Value}}</a></td>
			</tr>
		</table>
	</div>
</div>

